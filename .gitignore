code/
assets/
# runs/ is tracked (slurm .out, GSSR PDFs + CSVs, configs, logs); only the huge profiler traces and checkpoints are not
runs/**/trace_rank*.json
runs/**/*.pt
tools/
env/gam-venv/
env/gam-venv-freeze.txt
env/torch_extensions/
env/triton_cache/
env/*.tar
